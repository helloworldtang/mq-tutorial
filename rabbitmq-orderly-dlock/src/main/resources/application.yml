spring:
  application:
    name: rabbitmq-orderly-dlock

  datasource:
    url: jdbc:mysql://localhost:3306/rocketmq_orderly?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: opc.tang2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin2026
    vhost: /

  data:
    redis:
      host: localhost
      port: 6379

rabbitmq:
  exchange: dlock-order-exchange

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.example.common.entity

logging:
  level:
    com.example: DEBUG
    org.springframework.amqp: INFO
    org.redisson: INFO
