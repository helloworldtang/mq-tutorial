<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.common.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.example.common.entity.Order">
        <id property="id" column="id"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" resultMap="OrderResultMap">
        SELECT id, status, version, create_time, update_time
        FROM `order`
        WHERE id = #{orderId}
    </select>

    <insert id="insert" parameterType="com.example.common.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (status, version, create_time, update_time)
        VALUES (#{status}, #{version}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateStatusWithVersion">
        UPDATE `order`
        SET status = #{targetStatus},
            version = #{newVersion},
            update_time = NOW()
        WHERE id = #{orderId}
          AND version = #{oldVersion}
    </update>

</mapper>
